Collections.sort(vals, new Comparator<T>() {
    public int compare(T o1, T o2) {
        return o1.compareTo(o2);
    }
});

Arrays.sort(vals, new Comparator<T>() {  
    public int compare(T o1, T o2) {
        return o1.compareTo(o2);
    }  
});